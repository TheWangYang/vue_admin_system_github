import { Box } from "./geometry";
const id = 0
const name = ""
const uid = 0
const box = null
const annotationLabel = null

const AnnotationLabel = {
    id,
    name
}

//定义注释对象
const Annotation = {
    uid,
    box,
    annotationLabel
}


//向外暴露对象
export {AnnotationLabel, Annotation}

